# Account release deployment fix

The release build remains bounded but now permits a realistic Cloudflare cold-build duration. The globally mounted guest controller also avoids importing the full card catalogue.
