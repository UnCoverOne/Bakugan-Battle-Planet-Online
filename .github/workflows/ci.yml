name: Continuous Integration

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  profile-artwork-apply:
    if: github.event_name == 'pull_request' && github.head_ref == 'fix/profile-original-artwork-v2'
    runs-on: ubuntu-latest
    steps:
      - name: Check out profile artwork branch
        uses: actions/checkout@v4
        with:
          ref: fix/profile-original-artwork-v2
          fetch-depth: 0
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Capture verification diagnostics
        shell: bash
        run: |
          set +e
          python .github/scripts/apply-profile-original-artwork.py > .profile-verify.log 2>&1
          echo "PATCH_EXIT=$?" >> .profile-verify.log
          git diff --check >> .profile-verify.log 2>&1
          echo "DIFF_EXIT=$?" >> .profile-verify.log
          npm ci --ignore-scripts >> .profile-verify.log 2>&1
          echo "NPM_CI_EXIT=$?" >> .profile-verify.log
          node --import tsx --test tests/profile-customization.test.ts >> .profile-verify.log 2>&1
          echo "PROFILE_TEST_EXIT=$?" >> .profile-verify.log
          npm run lint >> .profile-verify.log 2>&1
          echo "LINT_EXIT=$?" >> .profile-verify.log
          git reset --hard HEAD
          git clean -fd
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          printf '%s\n' "$(cat .profile-verify.log 2>/dev/null)" > .profile-verify.log
          git add .profile-verify.log
          git commit -m "Capture profile artwork verification diagnostics"
          git push origin HEAD:fix/profile-original-artwork-v2

  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm test
      - name: Build
        run: npm run build
